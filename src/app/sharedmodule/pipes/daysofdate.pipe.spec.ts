import { DaysofdatePipe } from './daysofdate.pipe';

describe('DaysofdatePipe', () => {
  it('create an instance', () => {
    const pipe = new DaysofdatePipe();
    expect(pipe).toBeTruthy();
  });
});
